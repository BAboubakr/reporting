document.addEventListener('DOMContentLoaded',()=>{const root=document.getElementById('overview');if(!root)return;const signals=Array.isArray(window.signals)?window.signals:[];const events=Array.isArray(window.eventData)?window.eventData:[];const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const dates=signals.map(s=>Date.parse(s.detected||s.updated||s.published)).filter(Number.isFinite);const last=dates.length?new Date(Math.max(...dates)).toLocaleString('en-GB',{day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',timeZone:'Africa/Casablanca'})+' (Morocco time)':'Unavailable';const high=signals.filter(s=>s.fichtnerRelevance==='HIGH'||Number(s.actionabilityScore)>=75);const actionable=signals.filter(s=>Number(s.actionabilityScore)>=60);const evidence=signals.length?Math.round(signals.filter(s=>['official source','news source'].includes(s.evidenceLevel)).length/signals.length*100):0;const cards=[...signals].sort((a,b)=>(Number(b.actionabilityScore)||0)-(Number(a.actionabilityScore)||0)).slice(0,4).map(s=>`<article class="intel-card"><div class="intel-card-top"><span class="signal-type">${esc((s.signalType||'MARKET SIGNAL').toUpperCase())}</span><span class="priority ${s.fichtnerRelevance==='HIGH'?'high':''}">${esc(s.fichtnerRelevance||'WATCH')}</span></div><h3>${esc(s.title||s.headline)}</h3><p>${esc(s.whyItMatters||s.summary)}</p><div class="intel-card-foot"><span>${esc(s.source||'Source evidence')}</span><a href="${esc(s.url||'#')}" target="_blank" rel="noopener">Open evidence →</a></div></article>`).join('');root.innerHTML=`<div class="command-head"><div><p class="eyebrow">ATLAS · MOROCCO MARKET COMMAND CENTER</p><h1>What changed in Morocco's<br><em>renewable-energy market?</em></h1><p>Signals are ranked by relevance and actionability so you can decide what deserves attention.</p></div><div class="freshness"><span class="live-dot"></span><b>Monitoring live</b><small>${signals.length} signals currently indexed</small><small class="last-update">Last update: ${last}</small></div></div><div class="command-kpis"><div><span>NEW SIGNALS</span><b>${signals.length}</b><small>indexed by Atlas</small></div><div><span>HIGH PRIORITY</span><b>${high.length}</b><small>requires attention</small></div><div><span>ACTIONABLE</span><b>${actionable.length}</b><small>score ≥ 60</small></div><div><span>EVIDENCE COVERAGE</span><b>${evidence}%</b><small>source-backed records</small></div></div><section class="command-section"><div class="section-title-v3"><div><span>01 · MARKET SIGNALS</span><h2>What needs your attention?</h2></div></div><div class="intel-grid">${cards||'<div class="empty-v3">No signals available.</div>'}</div></section><div class="command-two"><section class="command-panel"><div class="section-title-v3"><div><span>02 · MARKET ACTIVITY</span><h2>Signals by theme</h2></div></div><div class="theme-list-v3"><div class="theme-row-v3"><span>Solar &amp; BESS</span><div></div><b>${signals.filter(s=>/solar|pv|bess|battery|storage/i.test((s.title||'')+' '+(s.summary||''))).length}</b></div><div class="theme-row-v3"><span>Grid &amp; regulation</span><div></div><b>${signals.filter(s=>/grid|onee|anre|regulation|transmission/i.test((s.title||'')+' '+(s.summary||''))).length}</b></div><div class="theme-row-v3"><span>Hydrogen &amp; PtX</span><div></div><b>${signals.filter(s=>/hydrogen|ammonia|ptx|electrolysis/i.test((s.title||'')+' '+(s.summary||''))).length}</b></div></div></section><section class="command-panel"><div class="section-title-v3"><div><span>03 · RELATIONSHIPS</span><h2>Upcoming engagement</h2></div></div><div class="connect-list">${events.slice(0,4).map(e=>`<article class="connect-card"><div class="connect-date"><b>${esc(e.day||'—')}</b><span>${esc(e.month||'TBD')}</span></div><div><span class="connect-label">ENGAGEMENT</span><h3>${esc(e.name||e.title||'Energy transition engagement')}</h3><p>${esc(e.detail||e.description||'Relevant stakeholder engagement.')}</p></div></article>`).join('')||'<div class="empty-v3">No upcoming engagements recorded.</div>'}</div></section></div><section class="command-section"><div class="section-title-v3"><div><span>04 · DECISION QUEUE</span><h2>Signals needing a decision</h2></div></div><div class="decision-grid-v3">${high.slice(0,3).map(s=>`<article class="decision-v3"><span>${esc((s.signalType||'SIGNAL').toUpperCase())}</span><strong>${esc(s.title||s.headline)}</strong><p>${esc(s.whyItMatters||s.summary)}</p></article>`).join('')||'<div class="empty-v3">No high-priority decisions currently queued.</div>'}</div></section>`});